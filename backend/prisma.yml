endpoint: https://eu1.prisma.sh/marciano-schildmeijer-5bbae1/lifthub/dev
datamodel: datamodel.prisma

generate:
  - generator: javascript-client
    output: ./generated

hooks:
  post-deploy:
    - graphql get-schema -p lifthub
